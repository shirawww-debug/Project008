# Coeur du projet (parsing BFI + baseline ML) — tourne sans matériel ni GPU.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# Interface web (bfid/web, commande `bfid web`) :
flask>=3.0
matplotlib>=3.7

# Optionnel — CNN temporel (bfid/model.py) :
#   pip install torch
# Optionnel — lecture de pcaps (bfid/pcap.py, commandes `parse`/`label`) :
#   pip install scapy
# Développement :
#   pip install pytest
